---
title: Messages
---

## Create a new message

The `create` method generates a new message based on the options provided.

```typescript
import { Lira } from 'lira'

const lira = new Lira()

const res = await lira.messages.create()
```

See the [options](/sdk-reference/messages/input) section for more details.
